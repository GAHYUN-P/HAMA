import React from 'react';

const RequestCenter = (props) => {

    return (
        <React.Fragment>
            <div style={{ width:'90%', margin:'10px auto 0' }} >
                <div style={{display:'inline-block'}} >좋아요</div>
                <div style={{display:'inline-block'}} >응답 갯수</div>
            </div>
        </React.Fragment>
    )
}

export default RequestCenter;