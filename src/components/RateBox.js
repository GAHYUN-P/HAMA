import React from "react";

const RateBox = (props) => {

    return (
        <React.Fragment>
            <div style={{padding:'10px 0'}} >
                <div style={{ display:'flex' }} >
                    <div>입력해 주세요</div>
                    <button>등록</button>
                </div>
                <div>
                    대충 별 별 별 별 별
                </div>
            </div>
        </React.Fragment>
    )
};

export default RateBox;