import React from 'react';

import Header from '../components/Header';
import RankList from '../components/RankList';

const Home = (props) => {
  return (
    <div>
      <Header/>
      <RankList/>
    </div>
  );
};

export default Home;
